<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Забруднення</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
  {{> header}}
  <main>
    <section class="pt-5">
      <div class="container">
        <h1 class="text-center">Редагування</h1>
        <form class="pt-3 d-flex flex-column gap-4 col-12 col-sm-9 col-md-8 col-lg-7 col-xl-6 mx-auto"
          action="/pollutions/{{pollution.pollution_id}}?_method=PUT" method="POST">
          <div>
            <label class="form-label" for="objectName">Підприємство <span class="text-danger">*</span></label>
            <select id="pollutionObject" name="pollution[object_name]" class="form-select" required="required">
              {{#each objects}}
              <option value="{{this.object_name}}" {{#if (eq this.object_name
                ../pollution.object_name)}}selected{{/if}}>
                {{this.object_name}}
              </option>
              {{/each}}
            </select>
          </div>
          <div>
            <label class="form-label" for="pollutionPollutant">Забруднювач <span class="text-danger">*</span></label>
            <select id="pollutionPollutant" name="pollution[pollutant_name]" class="form-select" required="required">
              {{#each pollutants}}
              <option value="{{this.pollutant_name}}" {{#if (eq this.pollutant_name
                ../pollution.pollutant_name)}}selected{{/if}}>
                {{this.pollutant_name}}
              </option>
              {{/each}}
            </select>
          </div>

          <div>
            <label class="form-label" for="pollutionYear">Рік</label>
            <input id="pollutionYear" class="form-control" type="number" min="2000" max="2023" step="1"
              name="pollution[pollution_year]" value="{{pollution.pollution_year}}" required="required" />
          </div>
          <div>
            <label class="form-label" for="pollutionValue">Кількість викидів (т/рік)</label>
            <input id="pollutionValue" class="form-control" type="number" step="any" min="0"
              name="pollution[pollution_value]" value="{{pollution.pollution_value}}" required="required" />
          </div>
          <button class="btn btn-primary" type="submit">Підтвердити</button>
        </form>
      </div>
    </section>
  </main>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous">
    </script>
  <script src="\js\activeNavItem.js"></script>
</body>

</html>